apiVersion: v1
kind: Service
metadata:
  name: {{ include "quote-semantic-search.fullname" . }}
  labels:
    {{- include "quote-semantic-search.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quote-semantic-search.selectorLabels" . | nindent 4 }}
